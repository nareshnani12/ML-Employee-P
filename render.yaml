services:
  - type: web
    name: ml-employee-productivity
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir "employee ml/Flask" app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
